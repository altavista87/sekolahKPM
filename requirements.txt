# Core dependencies
python-telegram-bot>=20.7
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic>=2.5.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0
alembic>=1.12.0

# Redis (for caching/sessions)
redis>=5.0.0

# Image processing
pillow>=10.1.0
pillow-heif>=0.14.0

# HTTP client
httpx>=0.25.0
aiohttp>=3.9.0

# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.6

# Utilities
python-dotenv>=1.0.0
tenacity>=8.2.0
python-dateutil>=2.8.0

# AI/ML (for OCR and processing)
google-generativeai>=0.3.0

# Note: Netlify Functions have size limits
# Some heavy dependencies may need to be loaded dynamically
# or use Netlify's larger function size tier
